#!/usr/bin/python3
"""
    is the object is an instance of or
    if is an instance of a lass that 
    inherited from
"""


def is_kind_of_class(obj, a_class):
    """function that defines if an obj
        is instance f or inst of 
        inherited from
    """

    return isinstance(obj, a_class)
